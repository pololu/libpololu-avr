; This file causes the Emacs text editor to follow the correct indentation style.
; For these templates, we follow the default indentation style of AVR Studio,
; which uses tabs, and each tab is 2 characters.

(
  (c-mode . ((c-basic-offset . 2) (tab-width . 2) (indent-tabs-mode . t)) )
)

